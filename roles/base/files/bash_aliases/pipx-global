# Install/manage a package for systemwide use.
# Until this or something like it gets accepted 
#   https://github.com/pypa/pipx/pull/1088
alias pipx-global='sudo PIPX_HOME=/opt/pipx PIPX_BIN_DIR=/usr/local/bin pipx'
